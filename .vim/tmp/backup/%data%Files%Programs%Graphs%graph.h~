#include <stdio.h>
#include <stdlib.h>

typedef struct node{
	float value;
	int index;
	int number_nodes;
	struct node ** nodes;
}node;

void createNode(node * n, float value, int index);

void addNodeToNode(node * n, node * m);
