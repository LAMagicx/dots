#include "graph.h"

node graph, m;

int main(int argc, char ** argv) {
	createNode(&graph, 1, 0);
	createNode(&m, 2, 1);
	addNodeToNode(&graph, &m);
	printf("%f\n", graph.value);
	printf("%f\n", graph.nodes[0]->value);
	return 0;
}
