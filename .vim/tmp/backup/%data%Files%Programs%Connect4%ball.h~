#ifndef __BALL_H__
#define __BALL_H__

#include "sdlinc.h"

// ball struct
typedef struct {
	int x;
	int y;
	float vx;
	float vy;
	float ax;
	float ay;
	int r;
}ball;

ball * balls = NULL;
int ballCount = 0;

#endif
