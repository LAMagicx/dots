fib :: Int -> Int
fib 0 = 0
fib 1 = 1
fib n = fib(n-1) + fib(n-2)

fi n = round((a^n - b^n) / (a - b))
  where 
    a = (1 + sqrt(5)) / 2
    b = (1 - sqrt(5)) / 2


fi :: Integer -> Integer
f n = go n (0,1)
  where 
    go n (a, b) | n == 0 = a
                | n /= 0  = go (n-1) (b, a+b)

main = do
print (123456789^98765432)
