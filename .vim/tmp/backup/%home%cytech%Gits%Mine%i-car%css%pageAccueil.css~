.icone{
  position: relative;
  margin: 10px;
  display: flex;
  flex-direction: column;
  text-align: center;
  transition: all ease-in-out 200ms;
}

.icone:hover{
	transform: scale(1.05);
}

.icone p{
  width: 100%;
  top: 20%;
  text-align: center;
  position: absolute;
  color: white;
  filter: drop-shadow(1px 2px 3px black);
  font-size: 2.6vw;
  font-weight: bold;
  display: none;
}

.icone:hover img{
  height: 250px;
  filter: saturate(0.4) blur(2px) brightness(0.8);
}

.icone:hover p{
  display: block;
  cursor: pointer;
}

.icone > a > p {
	transform: translateX(-4%);
	margin-top: 10px;
}

.menu_icone{
  margin-left: 5%;
  margin-right: 5%;
  display: flex;
  flex-direction: row ;
  justify-content: space-between;
}

@media (max-width: 1000px) {
	.menu_icone {
		flex-flow: row wrap;
	}

}
